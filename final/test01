fdef sum(i: int, j:int) : int {
     return i + j;
}
	
fdef sum(i:float, j:float) : float {
     return i + j;
}


{

  s1 : int = sum(-10,20);
  s2 : float = sum(10.0,-20.0);

  s = 0f;
  if (s1 < s2 || s1 == s2) {
     b : bool =  s1 + s2 / (s1 + s2) >= 30;
  } else {
  sdf= 2 in a[1,2,3];
     /* do something else */
  }
}